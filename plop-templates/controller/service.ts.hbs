import { {{ singularCaps controller}}, {{ singularCaps controller}}Input } from '../../../models/{{ singularCamel controller}}.model'

export const getAll{{ singularCaps controller}}s = async () => {
  return await {{ singularCaps controller}}.find()
}

export const create{{ singularCaps controller}} = async (new{{ singularCaps controller}}: {{ singularCaps controller}}Input) => {
  return await {{ singularCaps controller}}.create(new{{ singularCaps controller}})
}

export const get{{ singularCaps controller}}ById = async (id: string) => {
  return await {{ singularCaps controller}}.findById(id)
}

export const update{{ singularCaps controller}} = async (id: string, updated{{ singularCaps controller}}: {{ singularCaps controller}}Input) => {
  return await {{ singularCaps controller}}.findByIdAndUpdate(id, updated{{ singularCaps controller}})
}

export const delete{{ singularCaps controller}} = async (id: string) => {
  return await {{ singularCaps controller}}.findByIdAndDelete(id)
}

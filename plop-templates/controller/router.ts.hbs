import { Router } from 'express'

import {
  getAll{{ pluralizePascal controller }},
  create{{ singularPascal controller}},
  get{{ singularPascal controller}}ById,
  update{{ singularPascal controller}},
  delete{{ singularPascal controller}},
} from './{{ pluralizeCamel controller }}.controller'

const router = Router()

router.route('/').get(getAll{{ pluralizePascal controller }}).post(create{{ singularPascal controller}})
router.route('/:id').get(get{{ singularPascal controller}}ById).put(update{{ singularPascal controller}}).delete(delete{{ singularPascal controller}})

export default router

import { Router } from 'express'

import {
  getAll{{ pluralizeCaps controller }},
  create{{ singularCaps controller}},
  get{{ singularCaps controller}}ById,
  update{{ singularCaps controller}},
  delete{{ singularCaps controller}},
} from './{{ pluralizeCamel controller }}.controller'

const router = Router()

router.route('/').get(getAll{{ pluralizeCaps controller }}).post(create{{ singularCaps controller}})
router.route('/:id').get(get{{ singularCaps controller}}ById).put(update{{ singularCaps controller}}).delete(delete{{ singularCaps controller}})

export default router
